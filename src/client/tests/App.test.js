import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

// TODO: The following test has not been test. Do it later
it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<App />, div);
});
